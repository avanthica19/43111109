select first_name, department, salary from worker order by department ASC, salary DESC;
select worker_id, first_name, salary from worker order by salary DESC;
SELECT department, COUNT(*) AS total_workers FROM worker GROUP BY department;
SELECT department, SUM(salary) AS total_salary FROM worker GROUP BY worker_id;
SELECT department, MIN(salary) AS min_salary, MAX(salary) AS max_salary FROM worker GROUP BY department;
SELECT department,worker_id, AVG(salary) AS avg_salary FROM worker GROUP BY department,worker_id ORDER BY avg_salary DESC;

SELECT department, MAX(worker_id) AS last_worker, AVG(salary) AS avg_salary FROM worker
GROUP BY department;

select department, sum(salary) as total from worker group by department;
SELECT department, AVG(salary) AS average_sal FROM worker GROUP BY department;
select department, avg(salary) as average group by department having max(average);


select count(*) from worker where department not in ('hr','admin');
select department, max(salary) as maximum_salary from worker group by department;

SELECT UPPER(first_name) AS name_in_upper FROM worker;
SELECT lower(first_name) AS name_in_lower FROM worker;
SELECT TRIM('   Hello World   ') AS trimm;
SELECT LTRIM('   SQL') AS left_trimmed, 
       RTRIM('SQL   ') AS right_trimmed;
       
SELECT worker_id, first_name FROM worker
ORDER BY worker_id
LIMIT 3 OFFSET 2;

SELECT department, AVG(salary) AS avg_salary
FROM worker
GROUP BY department
ORDER BY avg_salary DESC
LIMIT 2 OFFSET 1;

select department , avg(salary) as average from worker group by department having avg(salary)>100000;
select department , max(salary) as maximum from worker group by department having max(salary)=500000;
select department from worker group by department having count(*)>1 and sum(salary)>100000;
select department from worker group by department having avg(salary) between  50000 and 200000;

create table award_winners(
id int,
name varchar(25),
award_name varchar(30));

insert into award_winners values(001,'Karthick','Volleyball winners'),(002,'Gopi','Basketball Runners'),(003,'Adib','Oscar winners'),
(004,'Harij','English SpellBee 2nd Runner-up'),(005,'Loki','Volleyball winners'),(006,'Sakthi','Badminton Nationals runners'),
(007,'Iqhlas','Debug winners'),(008,'AK','Basketball runners'),(009,'Kamal','Hackathon runners'),
(010,'Barath','Hackathon winners');

select * from award_winners;

create table students(
id int,
name varchar(25));

insert into students values(005,'Loki'),(1001,'Srihari'),(1003,'Dinesh'),(001,'Karthick'),(1005,'Tharun'),
(1006,'Vamsi'),(1007,'Monish'),(1008,'Rohith'),(1009,'Alwyn'),(1010,'Akash');

select * from students;
